<!-- Gap ledger for this repo: the source of truth for its open gaps. Managed with scripts/gaps-ledger.mjs in the Almadar monorepo. -->
# @almadar/runtime — open gaps

Every open gap this repo owns lives here. This file is the source of truth; the monorepo's `docs/Almadar_Gaps.md` only rolls it up.

- **One entry per gap:** `- **<code>** — <what is wrong and where>. <owning package> [mechanical|architectural] — <evidence, prevention rung>`. `[mechanical]` = small and well-scoped; `[architectural]` = needs design judgment.
- **Codes:** new gaps use this repo's prefix `G-RUNTIME-`. Take the "Next code" below, then bump it in the same edit. Codes are never reused or renamed.
- **Close by deleting.** Remove the entry in the same commit as the fix. There is no "closed" section; git history is the record.
- **Cross-repo gaps don't go here.** If fixing it needs another repo, describe it in your report or PR body; the monorepo coordinator files it.

Next code: `G-RUNTIME-050`

## Open gaps

### Runtime / Server tier (`@almadar/runtime`, `@almadar-io/playground-runtime`, hosted `apps/builder` server)

- **G-RUNTIME-047** — Mount lifecycle on the stateless host covers client-only traits only. The client fold now holds a server-run delivery for a `hybridClientOnly` trait still awaiting INIT and replays it after INIT (fixed 2026-09-25, `client-fold-holds-until-init.test.ts`). A SERVER-BACKED trait awaiting INIT is still run early by the stateless host (no persistent mount; `_awaitingInit` is ignored without `deps.mount`), and its later INIT leg can reset it. A client-side hold would drop the delivery's server work (the INIT leg replays only INIT), so this needs a design: carry held deliveries on the INIT leg, or a stateless `_awaitingInit` skip-and-return contract. Prevention rung: 3 (a stateless mount-order case in runtime-verify). `@almadar/runtime` `[architectural]`
- **G-RUNTIME-006** — UNCERTAIN (2026-09-21 sweep): a related commit (`e55c1f6 fix(kernel): reconcile transient-closure tolerance...`) touches the exact file this gap names, but doesn't add the specific "failure-injection" mechanism the gap calls for — unclear if it already resolved the false-FAIL or is unrelated tolerance work. Needs a live `verify_runtime --trait ListItemBrowse` re-run to settle before assignment. `[medium, needs live re-verification first]` — orig: `Almadar_Runtime_Gaps.md` — PLANNED 2026-09-21: settle first via live `verify_runtime --trait ListItemBrowse` re-run before assignment.
- **G-RUNTIME-008** — CSV file content → parsed rows has no emitter and can't get one without a base64-decode+CSV-parse evaluator capability. `[substrate work, owner consult required]` — orig: `Almadar_Runtime_Gaps.md`
- **G-RUNTIME-009** — NOT REPRODUCED 2026-09-25: 12/12 fresh loads (6 per topology) deliver ArrowRight to `RiyaOwPlay` in the jsdom harness (`Ledger.top-priority-repro.test.tsx`), and the suspected listener churn is fixed (`MathCanvas` memoizes `keyMap`). Close after one real-browser multi-load `app_play` check, since jsdom has no render churn. Nondeterministic (~60–75%) event loss on an 11-trait orbital under real load. `[high — race condition, needs deep instrumentation]` — orig: `Almadar_Compiler_Gaps.md` → §47 — PLANNED 2026-09-21 (wave 4): deterministic actor-queue instrumentation (StateMachineCore.ts queues/processing sets, compositeKey scopes; suspects: concurrent same-scope drains, event-queue-coalesce.ts, bridge echo races), reproduce under load, fix, instrumentation kept behind a debug flag. Largest item, timeboxed.
- **G-RUNTIME-013** — Two hosted-only Firestore-seed defects (empty project-template titles, empty asset-review-queue titles). `[blocked on hosted access]` — orig: `Almadar_Runtime_Gaps.md` — RE-VERIFIED 2026-09-21: no seed script for project-template/asset-review-queue exists anywhere repo-side (grep matches only this ledger); unverifiable without hosted access, no repo-side fix possible.
- **G-RUNTIME-016** — Workspace tree lives on ephemeral gen1 storage, sharing the 2048Mi budget. `[medium-high, infra]` — orig: `Almadar_Studio_Gaps.md` → S4 — RE-VERIFIED 2026-09-21 STILL DEFECT: apphosting.yaml runConfig has no volume mount; repo-wide search finds no persistent-storage/gen2 config. Wave: audit runtime workspace-tree contents, ship cheapest durable home (Firestore/GCS) if contained, else document.
- **G-RUNTIME-033** — CLIENT FRAMES ARE KEYED PER TRAIT, NOT PER ENTITY INSTANCE: `CircuitStore.frames` is keyed by `IndexedTrait.frameKey` (one row per trait; `[shared]` entities share one key), while Rust's client entity source is keyed by (linked-entity type, id). `client-role.ts` `fanOutEntityRows` bridges it by merging the rows of the traits a dispatch WROTE into every same-entity/same-id sibling frame, last, but a stateful server's echo rows for siblings it never fanned still land in their own frames stale, and a fold's `response.data` (fetched collection rows) still has no destination. Prevention rung: runtime JS↔Rust parity (the three-seeding suite should compare sibling frames after a round trip). Fix: key frames by (entity, id) in `CircuitStore` and resolve each trait's row through its binding. `[architectural]` — orig: W5b hook split review 2026-09-23
